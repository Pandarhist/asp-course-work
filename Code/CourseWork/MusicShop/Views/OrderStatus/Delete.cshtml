@model MusicShop.Models.OrderStatus

@{
    ViewData["Title"] = "Удаление статуса заказа";
}

<div class="row mx-auto w-75">
    <h4>Вы уверены, что хотите удалить этот статуса?</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-4">
            Код
        </dt>
        <dd class="col-sm-8">
            @Html.DisplayFor(model => model.Id)
        </dd><dt class="col-sm-4">
            Статус
        </dt>
        <dd class="col-sm-8">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger w-100 p-1 mt-2" />
        <a asp-action="Index" class="btn btn-secondary w-100 p-1 mt-2">Вернуться к статусам</a>
    </form>
</div>
